BOT_TOKEN=replace_with_token_from_BotFather
DB_PATH=data/finance.db
TIMEZONE=Europe/Moscow
# false: only IDs below can use the budget; true: anyone can open a personal budget with /start.
PUBLIC_SIGNUP=false
# Comma-separated Telegram IDs for closed access and existing users.
# Keep existing IDs when enabling self-service. Empty + PUBLIC_SIGNUP=false = setup mode.
ALLOWED_USER_IDS=
